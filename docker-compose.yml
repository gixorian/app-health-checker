name: nerva-engine

services:
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - api
    restart: always

  api:
    build: .
    image: nerva:latest
    volumes:
      - .:/app
    deploy:
      replicas: 1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
      - REDIS_HOST=${REDIS_HOST}
      - CELERY_BROKER_URL=${CELERY_BROKER_URL}
      - DATABASE_URL=${DATABASE_URL}
    user: "${USER_ID:-1000}:${GROUP_ID:-1000}"
    command: uvicorn nerva.api:app --host 0.0.0.0 --port 8000
    depends_on:
      - redis-db
      - postgres-db
    restart: always

  worker:
    image: nerva:latest
    volumes:
      - .:/app
    working_dir: /app
    deploy:
      replicas: 1
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
      - CELERY_BROKER_URL=${CELERY_BROKER_URL}
      - DATABASE_URL=${DATABASE_URL}
    user: "${USER_ID:-1000}:${GROUP_ID:-1000}"
    command: celery -A nerva.engine worker --loglevel=info
    depends_on:
      - redis-db
      - postgres-db
    restart: always

  dashboard:
    image: mher/flower:latest
    command: celery --broker=${CELERY_BROKER_URL} flower --port=5555
    ports:
      - "5555:5555"
    depends_on:
      - redis-db

  redis-db:
    image: redis:8-alpine
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - redis_data:/data

  postgres-db:
    image: postgres:18-alpine
    container_name: postgres-db
    environment:
      - POSTGRES_USER=appuser
      - POSTGRES_PASSWORD=apppassword
      - POSTGRES_DB=nerva_db
    volumes:
      - postgres_data:/var/lib/postgresql
    ports:
      - "5432:5432"

volumes:
  redis_data:
  postgres_data:
